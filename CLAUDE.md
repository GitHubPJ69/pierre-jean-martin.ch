# CLAUDE.md

This project uses **[`AGENTS.md`](AGENTS.md)** as the single source of truth for all coding-agent instructions. Edit that file, not this one. The line below imports its contents so Claude Code sees the same guidance as Codex, Cursor, and any other agent that reads `AGENTS.md` directly.

@AGENTS.md
